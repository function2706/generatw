diff --git a/Emuera/Runtime/Config/Config.cs b/Emuera/Runtime/Config/Config.cs
index 4dc3ee7..5dfb922 100644
--- a/Emuera/Runtime/Config/Config.cs
+++ b/Emuera/Runtime/Config/Config.cs
@@ -167,6 +167,11 @@ internal static class Config
 		CBScrollCount = instance.GetConfigValue<int>(ConfigCode.CBScrollCount);
 		CBMinTimer = instance.GetConfigValue<int>(ConfigCode.CBMinTimer);
 		#endregion
+		#region generatw連携 (画面テキストのsocket送信)
+		GTWSocketEmit = instance.GetConfigValue<bool>(ConfigCode.GTWSocketEmit);
+		GTWSocketHost = instance.GetConfigValue<string>(ConfigCode.GTWSocketHost);
+		GTWSocketPort = instance.GetConfigValue<int>(ConfigCode.GTWSocketPort);
+		#endregion
 		#region EmuEra-Rikaichan related settings
 		RikaiEnabled = instance.GetConfigValue<bool>(ConfigCode.RikaiEnabled);
 		RikaiFilename = instance.GetConfigValue<string>(ConfigCode.RikaiFilename);
@@ -649,6 +654,16 @@ internal static class Config
 	public static int CBScrollCount { get; private set; }
 	public static int CBMinTimer { get; private set; }
 	#endregion
+	#region generatw連携 (画面テキストのsocket送信)
+	public static bool GTWSocketEmit { get; private set; }
+	public static string GTWSocketHost { get; private set; }
+	public static int GTWSocketPort { get; private set; }
+	/// <summary>
+	/// 画面テキストのバッファリングが必要か (クリップボード連携 or generatw socket 連携).
+	/// ClipboardProcessor の行バッファ蓄積・送出の要否判定に用いる.
+	/// </summary>
+	public static bool CBBufferingActive => CBUseClipboard || GTWSocketEmit;
+	#endregion
 	#region EmuEra-Rikaichan related settings
 	public static bool RikaiEnabled { get; private set; }
 	public static string RikaiFilename { get; private set; }
diff --git a/Emuera/Runtime/Config/ConfigCode.cs b/Emuera/Runtime/Config/ConfigCode.cs
index b164f32..5ec2beb 100644
--- a/Emuera/Runtime/Config/ConfigCode.cs
+++ b/Emuera/Runtime/Config/ConfigCode.cs
@@ -197,4 +197,9 @@ internal enum ConfigCode
 	CBScrollCount,
 	CBMinTimer,
 	#endregion
+	#region generatw連携 (画面テキストのsocket送信)
+	GTWSocketEmit,
+	GTWSocketHost,
+	GTWSocketPort,
+	#endregion
 }
diff --git a/Emuera/Runtime/Config/ConfigData.cs b/Emuera/Runtime/Config/ConfigData.cs
index d01a9bd..0f642e9 100644
--- a/Emuera/Runtime/Config/ConfigData.cs
+++ b/Emuera/Runtime/Config/ConfigData.cs
@@ -165,6 +165,11 @@ internal sealed class ConfigData
 		configArray.Add(new ConfigItem<int>(ConfigCode.CBScrollCount, "スクロールの行数", "Clipboard- Scrolled Lines per Key", 5));
 		configArray.Add(new ConfigItem<int>(ConfigCode.CBMinTimer, "クリップボードの更新間隔(ミリ秒)", "Clipboard- min time between pastes", 800));
 		#endregion
+		#region generatw連携 (画面テキストのsocket送信)
+		configArray.Add(new ConfigItem<bool>(ConfigCode.GTWSocketEmit, "画面テキストをsocketで送信する(generatw連携)", "Generatw- Emit screen text over socket", false));
+		configArray.Add(new ConfigItem<string>(ConfigCode.GTWSocketHost, "generatw送信先ホスト", "Generatw- destination host", "127.0.0.1"));
+		configArray.Add(new ConfigItem<int>(ConfigCode.GTWSocketPort, "generatw送信先ポート", "Generatw- destination port", 52340));
+		#endregion
 		#region EmuEra-Rikaichan related settings
 		configArray.Add(new ConfigItem<bool>(ConfigCode.RikaiEnabled, "Rikaichanを使用する", "Rikai- Enabled", false));
 		configArray.Add(new ConfigItem<string>(ConfigCode.RikaiFilename, "Rikaichanのファイルパス", "Rikai- Dictionary Filename", "Emuera-Rikai-edict.txt-eucjp"));
diff --git a/Emuera/Runtime/Script/Statements/Clipboard.cs b/Emuera/Runtime/Script/Statements/Clipboard.cs
index 240c385..25b953e 100644
--- a/Emuera/Runtime/Script/Statements/Clipboard.cs
+++ b/Emuera/Runtime/Script/Statements/Clipboard.cs
@@ -50,7 +50,10 @@ internal partial class ClipboardProcessor
 		NewLineCount = 0;
 		OldNewLineCount = 0;
 
-		if (!Config.Config.CBUseClipboard) return;
+		if (!Config.Config.CBBufferingActive) return;
+
+		if (Config.Config.GTWSocketEmit)
+			Utils.Generatw.GeneratwEmitter.Start(Config.Config.GTWSocketHost, Config.Config.GTWSocketPort);
 
 		Init();
 	}
@@ -131,7 +134,7 @@ internal partial class ClipboardProcessor
 	//FIXIT - Autoprocess old lines or just ditch?
 	public void AddLine(ConsoleDisplayLine inputLine, bool left)
 	{
-		if (!Config.Config.CBUseClipboard) return;
+		if (!Config.Config.CBBufferingActive) return;
 
 		NewLineCount++;
 		string processed = ProcessLine(inputLine.ToString());
@@ -140,7 +143,7 @@ internal partial class ClipboardProcessor
 
 	public void DelLine(int count)
 	{
-		if (!Config.Config.CBUseClipboard || count <= 0) return;
+		if (!Config.Config.CBBufferingActive || count <= 0) return;
 
 		NewLineCount = Math.Max(0, NewLineCount - count);
 		if (count >= lineBuffer.Count) lineBuffer.Clear();
@@ -156,7 +159,7 @@ internal partial class ClipboardProcessor
 
 	public void ClearScreen()
 	{
-		if (!Config.Config.CBUseClipboard) return;
+		if (!Config.Config.CBBufferingActive) return;
 		if (Config.Config.CBClearBuffer)
 		{
 			lineBuffer.Clear();
@@ -171,7 +174,7 @@ internal partial class ClipboardProcessor
 
 	public void Check(CBTriggers type)
 	{
-		if (!Config.Config.CBUseClipboard) return;
+		if (!Config.Config.CBBufferingActive) return;
 		switch (type)
 		{
 			case CBTriggers.LeftClick:
@@ -204,7 +207,7 @@ internal partial class ClipboardProcessor
 
 	private void SendToCB(bool force)
 	{
-		if (!Config.Config.CBUseClipboard) return;
+		if (!Config.Config.CBBufferingActive) return;
 		if (NewLineCount == 0 && !force) return;
 		if (!MinTimeCheck())
 		{
@@ -230,7 +233,10 @@ internal partial class ClipboardProcessor
 		if (newText == OldText) return;
 		try
 		{
-			mainWin.Invoke(() => Clipboard.SetDataObject(newText, false, 3, 200));
+			if (Config.Config.CBUseClipboard)
+				mainWin.Invoke(() => Clipboard.SetDataObject(newText, false, 3, 200));
+			if (Config.Config.GTWSocketEmit)
+				Utils.Generatw.GeneratwEmitter.Send(newText);
 			if (ScrollPos == 0) OldNewLineCount = NewLineCount;
 			NewLineCount = 0;
 			OldText = newText;
diff --git a/Emuera/Runtime/Utils/Generatw/GeneratwEmitter.cs b/Emuera/Runtime/Utils/Generatw/GeneratwEmitter.cs
new file mode 100644
index 0000000..65499f3
--- /dev/null
+++ b/Emuera/Runtime/Utils/Generatw/GeneratwEmitter.cs
@@ -0,0 +1,165 @@
+using System;
+using System.Collections.Concurrent;
+using System.Net.Sockets;
+using System.Text;
+using System.Threading;
+
+namespace MinorShift.Emuera.Runtime.Utils.Generatw;
+
+/// <summary>
+/// generatw (画像生成アプリ) との連携用に, 画面テキストを TCP で push するエミッタ.
+///
+/// Emuera は TCP クライアントとして 127.0.0.1:&lt;port&gt; へ接続し, 画面確定時
+/// (ClipboardProcessor と同じトリガ) の可視テキストを送る.
+/// メッセージフレーム: 4byte 長さ (big-endian, 符号なし) + UTF-8 本文.
+///
+/// 送信はすべてバックグラウンドのワーカースレッドで best-effort に行い,
+/// 接続失敗・切断・例外はすべて握り潰してゲーム進行を一切阻害しない.
+/// アプリ未起動時は送信が黙って捨てられる.
+/// </summary>
+public static class GeneratwEmitter
+{
+	// 画面テキストは「最新の状態のみ」重要なので, 詰まった場合は古いものを捨てる.
+	private const int QueueCapacity = 64;
+
+	private static readonly object gate = new();
+	private static BlockingCollection<string> queue;
+	private static Thread worker;
+	private static volatile bool started;
+
+	private static string host = "127.0.0.1";
+	private static int port = 52340;
+
+	private static TcpClient client;
+	private static NetworkStream stream;
+
+	/// <summary>
+	/// エミッタを起動する (多重起動は無視). Config 確定後に一度呼ぶ.
+	/// </summary>
+	public static void Start(string emitHost, int emitPort)
+	{
+		lock (gate)
+		{
+			if (started) return;
+			host = string.IsNullOrEmpty(emitHost) ? "127.0.0.1" : emitHost;
+			port = emitPort;
+			queue = new BlockingCollection<string>(QueueCapacity);
+			worker = new Thread(WorkerLoop)
+			{
+				IsBackground = true,
+				Name = "GeneratwEmitter",
+			};
+			started = true;
+			worker.Start();
+		}
+	}
+
+	/// <summary>
+	/// 画面テキストを送信キューへ積む. 未起動なら何もしない.
+	/// キューが一杯の場合は古いものを捨てて最新を優先する.
+	/// </summary>
+	public static void Send(string text)
+	{
+		if (!started || text == null) return;
+		var q = queue;
+		if (q == null) return;
+
+		if (!q.TryAdd(text))
+		{
+			// 詰まっている: 古いものを 1 件捨てて最新を入れ直す (best-effort)
+			q.TryTake(out _);
+			q.TryAdd(text);
+		}
+	}
+
+	/// <summary>
+	/// 終了処理. ワーカーを止めて接続を閉じる.
+	/// </summary>
+	public static void Stop()
+	{
+		lock (gate)
+		{
+			if (!started) return;
+			started = false;
+			try { queue?.CompleteAdding(); } catch (Exception) { }
+		}
+	}
+
+	private static void WorkerLoop()
+	{
+		try
+		{
+			foreach (var text in queue.GetConsumingEnumerable())
+			{
+				TrySend(text);
+			}
+		}
+		catch (Exception)
+		{
+			// GetConsumingEnumerable が CompleteAdding 等で抜ける場合を含め, 握り潰す
+		}
+		finally
+		{
+			CloseConnection();
+		}
+	}
+
+	private static void TrySend(string text)
+	{
+		try
+		{
+			EnsureConnected();
+			if (stream == null) return;
+
+			byte[] body = Encoding.UTF8.GetBytes(text);
+			byte[] frame = new byte[4 + body.Length];
+			// 4byte big-endian 符号なし長さ (Python 側 struct ">I" と一致)
+			uint len = (uint)body.Length;
+			frame[0] = (byte)((len >> 24) & 0xFF);
+			frame[1] = (byte)((len >> 16) & 0xFF);
+			frame[2] = (byte)((len >> 8) & 0xFF);
+			frame[3] = (byte)(len & 0xFF);
+			Buffer.BlockCopy(body, 0, frame, 4, body.Length);
+
+			stream.Write(frame, 0, frame.Length);
+		}
+		catch (Exception)
+		{
+			// 送信失敗: 接続を破棄し, 次回再接続を試みる
+			CloseConnection();
+		}
+	}
+
+	private static void EnsureConnected()
+	{
+		if (client != null && client.Connected && stream != null) return;
+
+		CloseConnection();
+		try
+		{
+			client = new TcpClient();
+			// 相手 (アプリ) 不在時に長時間ブロックしないよう即時接続を試みる
+			var ar = client.BeginConnect(host, port, null, null);
+			if (!ar.AsyncWaitHandle.WaitOne(TimeSpan.FromMilliseconds(300)))
+			{
+				CloseConnection();
+				return;
+			}
+			client.EndConnect(ar);
+			client.NoDelay = true;
+			stream = client.GetStream();
+		}
+		catch (Exception)
+		{
+			CloseConnection();
+		}
+	}
+
+	private static void CloseConnection()
+	{
+		try { stream?.Dispose(); } catch (Exception) { }
+		try { client?.Close(); } catch (Exception) { }
+		stream = null;
+		client = null;
+	}
+}
diff --git a/Emuera/UI/Framework/Forms/MainWindow.cs b/Emuera/UI/Framework/Forms/MainWindow.cs
index 2d6438d..9214cc7 100644
--- a/Emuera/UI/Framework/Forms/MainWindow.cs
+++ b/Emuera/UI/Framework/Forms/MainWindow.cs
@@ -1517,7 +1517,8 @@ namespace MinorShift.Emuera.Forms
 		{
 			if (クリップボードにコピーToolStripMenuItem.Checked)
 				GlobalStatic.Console.CBProc.Init();
-			else
+			else if (!Config.GTWSocketEmit)
+				// generatw socket連携中はバッファを維持する
 				GlobalStatic.Console.CBProc.Reset();
 			ConfigData.Instance.GetConfigItem(ConfigCode.CBUseClipboard).SetValue(クリップボードにコピーToolStripMenuItem.Checked);
 			Config.SetConfig(ConfigData.Instance);
diff --git a/Emuera/UI/Game/EmueraConsole.Print.cs b/Emuera/UI/Game/EmueraConsole.Print.cs
index 27079ab..9509d89 100644
--- a/Emuera/UI/Game/EmueraConsole.Print.cs
+++ b/Emuera/UI/Game/EmueraConsole.Print.cs
@@ -166,7 +166,7 @@ internal sealed partial class EmueraConsole : IDisposable
 	private void addDisplayLine(ConsoleDisplayLine line, bool force_LEFT)
 	{
 		#region EE_AnchorのCB機能移植
-		if (Config.CBUseClipboard)
+		if (Config.CBBufferingActive)
 			CBProc.AddLine(line, force_LEFT);
 		#endregion
 		if (LastLineIsTemporary)
@@ -253,7 +253,7 @@ internal sealed partial class EmueraConsole : IDisposable
 	public void deleteLine(int argNum)
 	{
 		#region EE_AnchorのCB機能移植
-		if (Config.CBUseClipboard)
+		if (Config.CBBufferingActive)
 			CBProc.DelLine(Math.Min(argNum, displayLineList.Count)); //FIXIT - Do we need to worry about the count?
 		#endregion
 		int delNum = 0;
diff --git a/Emuera/UI/Game/EmueraConsole.cs b/Emuera/UI/Game/EmueraConsole.cs
index dd8dd51..9feb4ca 100644
--- a/Emuera/UI/Game/EmueraConsole.cs
+++ b/Emuera/UI/Game/EmueraConsole.cs
@@ -637,7 +637,7 @@ internal sealed partial class EmueraConsole : IDisposable
 	public void WaitInput(InputRequest req)
 	{
 		#region EE_AnchorのCB機能移植
-		if (Config.CBUseClipboard)
+		if (Config.CBBufferingActive)
 			CBProc.Check(ClipboardProcessor.CBTriggers.InputWait);
 		#endregion
 		state = ConsoleState.WaitInput;
@@ -661,7 +661,7 @@ internal sealed partial class EmueraConsole : IDisposable
 	public void ReadAnyKey(bool anykey = false, bool stopMesskip = false)
 	{
 		#region EE_AnchorのCB機能移植
-		if (Config.CBUseClipboard)
+		if (Config.CBBufferingActive)
 			CBProc.Check(ClipboardProcessor.CBTriggers.AnyKeyWait);
 		#endregion
 		InputRequest req = new();
