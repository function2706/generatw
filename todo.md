・TW：出かけた場合の場所取得
・画像ファイルの格納構造を規定(画像とメタデータをどう紐づける？)
　→そもそもメタデータを取り出せるようにしないといけない(yiのCopilotのログ参照)
　→CSV機構を作ったが, 毎度プロンプトからハッシュ値を計算するので, それをもとにディレクトリ名を走査するというのでもいいかも
　 アップスケール時に必要かとも思ったが, すでに画像にメタデータを埋め込んでいるのでこれもいらない公算大
・生成済みのメタデータに紐づく画像をランダムで表示する機構を追加
・画像群の更新仕様を規定, 新しい画像はいつ追加するか？またはボタン押下のみをトリガとするか？
・アップスケールデーモンを追加, アップスケールは時間がかかるので生成後にアトミックに行うのではなく, GUIに表示されていない段階で裏で逐次実施するようにする
→Good / Bad ボタンを追加, Good を押すとアップスケール対象にキュー, Badは削除対象 or 即時削除
削除対象になると最大数超過時に優先的に削除していくとか
・List, Dictに統一
・<と>ボタンの処理において、list[PicStats]でdirが一致するものを集めるだけで十分では